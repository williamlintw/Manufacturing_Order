# 製令紀錄回報台 (Manufacturing Order Tracking System)

這是一個基於 **React** (前端) 與 **Google Sheets** (後端資料庫) 的製令追蹤系統。透過 **Google Apps Script (GAS)** 作為 API 介面，實現製令的發出、首件檢驗、開工與完工紀錄。

## 📋 功能簡介

*   **製令發出** (`/issue`)：輸入製令編號並發出。
*   **首件檢驗** (`/qc`)：紀錄首件檢驗的時間。
*   **生產紀錄** (`/production`)：掃描或輸入製令編號進行開工/完工回報。
*   **查詢統計** (`/query`)：查詢製令的生產狀態與歷程 (預設首頁)。

## 🚀 快速開始 (前端)

本專案使用 Vite 建置。

### 環境需求
*   Node.js (建議 v16 以上)

### 安裝與執行

1.  **安裝依賴套件**
    ```bash
    npm install
    ```

2.  **啟動開發伺服器**
    ```bash
    npm run dev
    ```
    開啟瀏覽器訪問 `http://localhost:5173/`。

3.  **建置正式版本**
    ```bash
    npm run build
    ```

## ☁️ 後端設定 (Google Sheets & GAS)

本系統無需傳統資料庫，直接使用 Google 試算表存儲資料。

### Step 1: 建立 Google Sheet

1.  前往 [Google Sheets](https://sheets.google.com) 建立一個新的試算表。
2.  建立以下兩個工作表 (Sheet)，並設定第一列為標題 (欄位名稱)：

    **工作表 1 名稱：`製令前準備紀錄`**
    *   `A1`: 製令編號
    *   `B1`: 製令發出時間
    *   `C1`: 首件檢驗時間

    **工作表 2 名稱：`製令資料`**
    *   `A1`: ID
    *   `B1`: 製令編號
    *   `C1`: 開工時間
    *   `D1`: 完工時間
    *   `E1`: 數量

### Step 2: 部署 Google Apps Script

1.  在試算表中，點選上方選單的 **擴充功能 (Extensions)** > **Apps Script**。
2.  將本專案 `backend/code.gs` 檔案中的完整程式碼複製並貼上到編輯器中。
3.  點選右上角的 **部署 (Deploy)** > **新增部署作業 (New deployment)**。
4.  點選齒輪圖示，選擇 **網頁應用程式 (Web app)**。
5.  設定如下 (重要！)：
    *   **說明 (Description)**: (任意，例如 "v1")
    *   **執行身分 (Execute as)**: **我 (Me)**
    *   **誰可以存取 (Who has access)**: **任何人 (Anyone)**
        *   *注意：必須選「任何人」，前端才能透過 API 存取。*
6.  點選 **部署 (Deploy)**。
    *   若跳出「授權存取」視窗，請點選「審查權限」 -> 選擇帳號 -> 進階 -> 前往 (不安全) -> 允許。
7.  **複製** 產生的 **網頁應用程式網址 (Web App URL)** (以 `/exec` 結尾)。

## ⚙️ 系統連線設定

完成後端部署後，需將網址填入前端系統。

1.  啟動前端網頁 (`npm run dev`)。
2.  在瀏覽器網址列輸入手動進入設定頁面：`http://localhost:5173/#/settings`。
3.  在欄位中貼上剛才複製的 **Google Apps Script Web App URL**。
4.  點選 **儲存設定**。

現在系統即可正常運作！

## 📦 自動部署 (GitHub Pages)

本專案已設定 GitHub Actions 自動部署流程。

### 設定步驟

1.  將本專案上傳至 GitHub repository。
2.  進入 Repository 的 **Settings** > **Secrets and variables** > **Actions**。
3.  點選 **New repository secret**。
4.  輸入名稱：`VITE_GAS_APP_URL`。
5.  輸入內容：(您的 Google Apps Script Web App URL)。
6.  點選 **Add secret**。

### 觸發部署

當您將程式碼 `push` 到 `main` 或 `master` 分支時，GitHub Actions 會自動執行建置並將網頁部署到 `gh-pages` 分支。

**注意：** 您可能需要到 **Settings** > **Pages**，確認 Source 設定為 `Deploy from a branch` 並選擇 `gh-pages` 分支。
